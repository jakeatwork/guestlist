
	<%= simple_form_for(@list) do |f| %>
	  <%= f.error_notification %>

	  <div class="form-inputs">
	    <%= f.association :event %>
	  </div>

	  <div class="form-actions">
	    <%= f.button :submit %>
	  </div>
	<% end %>

