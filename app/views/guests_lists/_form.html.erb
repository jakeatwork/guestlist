<%= simple_form_for(@guests_list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :guest_id %>
    <%= f.input :list_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
